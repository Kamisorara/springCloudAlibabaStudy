spring:
  application:
    # 服务名称和配置文件保持一致
    name: user-service
  profiles:
    #环境也和配置文件保持一致
    active: dev
  cloud:
    nacos:
      config:
        #配置文件名后缀
        file-extension: yml
        #Nacos服务器地址
        server-addr: localhost:8848
